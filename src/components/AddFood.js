import React from "react";

function AddFood(){

    const displayFrom = () => {
        
    }

    return (
        <div className="AddFood">
            <button onClick={displayFrom}>Create new food</button>
        </div>
    )
}

export default AddFood;