import React, { Component } from 'react'

export default class Search extends Component {
    render() {
        return (
            <div className="field">
                <div className="control">
                    {/* <input className="input" type="text" placeholder="Search" value={this.state.title} onChange={(e) => this.handleChange(e)} /> */}
                </div>
            </div>
        )
    }
}
