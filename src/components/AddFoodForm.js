import { Divider } from 'antd';

function AddFoodForm({ foodListm }) {
  return (
    <form>
      <Divider> Add Food Entry</Divider>
      {/* <Input value={} type="text" onChange={} /> */}
    </form>
  );
}

export default AddFoodForm;
